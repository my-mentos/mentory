name: Discord Notification on Merge

on:
  pull_request:
    types: [closed]
    branches:
      - main

jobs:
  discord-notify:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord Notification
        uses: tsickert/discord-webhook@v5
        with:
          webhook-url: ${{ secrets.DISCORD_WEBHOOK_URL }}
          username: "깃허브 병합 알림 봇"
          avatar-url: "https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png"
          content: "🎉 Pull Request가 `main` 브랜치에 병합되었습니다!"

          embed-title: "${{ github.event.pull_request.title }}"
          embed-url: "${{ github.event.pull_request.html_url }}"

          embed-description: |
            병합자: **${{ github.actor }}**
            🔀 병합된 브랜치: **${{ github.event.pull_request.base.ref }}**
            🕒 병합 시각: **${{ github.event.pull_request.merged_at }} (UTC)**

          embed-color: 652801
