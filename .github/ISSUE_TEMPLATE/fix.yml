name: "🐛 Fix"
description: 버그 신고 및 수정 작업을 위한 템플릿
title: "[FIX] "
labels: ["fix", "bug"]
body:
  - type: textarea
    id: problem
    attributes:
      label: ⚠️ 문제 설명 (What happened?)
      placeholder: 어떤 문제가 발생했는지 자세히 설명하세요.
    validations:
      required: true

  - type: textarea
    id: reproduce
    attributes:
      label: 🔁 재현 절차 (Steps to Reproduce)
      placeholder: |
        1. 앱 실행
        2. 특정 화면 이동
        3. 버튼 클릭 → 크래시 발생
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: 👍 기대 동작 (Expected Behavior)
      placeholder: 문제 없이 정상 처리되어야 하는 동작
    validations:
      required: true

  - type: textarea
    id: logs
    attributes:
      label: 📸 로그 / 스크린샷
      placeholder: 크래시 로그, 오류 메시지 등을 첨부하세요.
